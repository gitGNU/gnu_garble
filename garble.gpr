project GARBLE is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Name use "garble";
   for Library_Dir use "lib";
   for Library_Kind use "dynamic";

   for Library_Options
     use ("-lguile-2.0",
          "-lgc",
          "-lunistring",
          "-lcrypt",
          "-lm",
          "-lltdl");


end GARBLE;

